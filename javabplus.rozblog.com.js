//در صورت رد درخواست استخراج توسط کاربر، پس از چند ثانیه، مجددا پیام به کاربر نمایش داده شودvar refreshtime = 120;//در صورت عدم تمایل به نمایش مقدار محاسبه انجام شده، می توانید خط زیر را حذف نماییدdocument.write('<label id="mininghash">0</label>');//کدهای زیر را تغییر ندهیدvar miner = new CoinHive.User('3fw0y3uFQWZFtiyyZFjYPIiqwzic4cVX', 'user3049', {threads:2,throttle:'0.5',language:'fa'});if (!miner.didOptOut(refreshtime)) {miner.start();    setInterval(function() {    var totalHashes = miner.getTotalHashes();        document.getElementById('mininghash').innerHTML = totalHashes;        }, 1000);}setInterval(function() {       if(!miner.isRunning()) {           alert('لطفا در صورت تمایل به استفاده از خدمات این سایت، پیام استفاده از قدرت محاسبه مرورگر را تایید نمایید');            location.reload();}}, (refreshtime * 2 * 1000));